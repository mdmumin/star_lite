let prefix = '';

let config = {
    all_page_title: prefix + " Managemnet",
    create_page_title: prefix + " Managemnet",
    edit_page_title: prefix + " Managemnet",
    detais_page_title: prefix + " Managemnet",

    route_name_prefix:  'glance',
    route_path_prefix : '/glance',

    api_host: 'http://127.0.0.1:8000',
    api_version: '/api/v1',
    api_prefix: '/user/auth-management',
}

export default config;